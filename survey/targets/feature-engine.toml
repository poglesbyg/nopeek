name = "feature-engine/WindowFeatures"
description = "Trailing rolling-window features over a single series, used as documented."
url = "https://github.com/feature-engine/feature_engine"
category = "library"

[source]
kind = "pypi"
package = "feature-engine"

[entry]
pipeline = "survey.adapters:feature_engine_window"
data = "survey.adapters:series"

[roles]
time = "t"
